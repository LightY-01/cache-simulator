# cache-simulator
Four cache replacement policies (LRU, LFU O(1), ARC, 2Q) implemented from scratch in C++, benchmarked on real UMass IoTrace disk access traces. Generates hit-rate vs cache-size curves. Identifies adversarial workloads per policy.
